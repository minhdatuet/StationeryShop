
import style from "./confirmingOrder.module.scss";
import clsx from 'clsx';

export const ConfirmingOrder = (props) => {
    
    return (
        <div className= {clsx(style.container)}>
            
        </div>
    )
};

export default ConfirmingOrder;